const stage = document.querySelector('#myCanvas');

export const SCREEN = {
  stage: stage,
  width: stage.width,
  height: stage.height,
};

export const MCQ = {
  MENUWIDTH: 200,
  MENUHEIGHT: 50,
}
